# Server Configuration
server.port=${PORT:${SERVER_PORT:8083}}

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Database Configuration
# Default: Neon PostgreSQL (temporary active DB)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://ep-super-bird-amsh9ogy-pooler.c-5.us-east-1.aws.neon.tech/neondb?user=neondb_owner&password=npg_HtTu1ymMNKh6&sslmode=require&channelBinding=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:neondb_owner}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:npg_HtTu1ymMNKh6}
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER_CLASS_NAME:org.postgresql.Driver}
spring.sql.init.mode=never

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL:true}
spring.jpa.properties.hibernate.dialect=${SPRING_JPA_DIALECT:org.hibernate.dialect.PostgreSQLDialect}
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

# JWT Configuration (Base64 encoded secret)
jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION:86400000}

# CORS Configuration
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:5176,http://localhost:3000}

# Logging
logging.level.org.springframework.security=${SPRING_SECURITY_LOG_LEVEL:DEBUG}
logging.level.com.resumebuilder=${APP_LOG_LEVEL:DEBUG}
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Hugging Face AI Configuration
huggingface.api.key=${HUGGINGFACE_API_KEY:}
huggingface.api.url=${HUGGINGFACE_API_URL:https://router.huggingface.co/v1}
huggingface.model=${HUGGINGFACE_MODEL:Qwen/Qwen2.5-7B-Instruct}

# RapidAPI Jobs Configuration
rapidapi.jobs.base-url=${RAPIDAPI_JOBS_BASE_URL:https://jsearch.p.rapidapi.com/search}
rapidapi.jobs.host=${RAPIDAPI_JOBS_HOST:jsearch.p.rapidapi.com}
rapidapi.jobs.key=${RAPIDAPI_JOBS_KEY:e4fb638178msh1d7c9c2937bc053p1c3ed9jsn57e97905fb87}

# Match Scoring Configuration
matching.ai-skill-extraction-enabled=${MATCHING_AI_SKILL_EXTRACTION_ENABLED:true}
